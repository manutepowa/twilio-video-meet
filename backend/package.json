{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "dev": "twilio-run --port 3001",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.3",
    "twilio": "3.77.0"
  },
  "devDependencies": {
    "twilio-run": "3.4.2"
  },
  "engines": {
    "node": "14"
  }
}
